# MI02: linearly decayed PPO profile for the MI02 task.
defaults:
  - SimToolRealMotionImitationMI01PPO
  - _self_

params:
  config:
    name: ${resolve_default:00_SimToolRealMotionImitationMI02PPO,${....experiment}}
    # rl_games linearly anneals this to its 1e-6 floor over max_epochs.
    learning_rate: 5e-5
    lr_schedule: linear
    schedule_type: standard
    # Decay to the rl_games 1e-6 floor over 6000 epochs, then keep training at
    # that floor until the user interrupts the process.
    lr_schedule_max_epochs: 6000
    max_epochs: -1
    max_frames: -1
