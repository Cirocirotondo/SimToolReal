# SAPG05 changes only the SAPG04 task reward coefficients.
defaults:
  - SimToolRealMotionImitationSAPG04PPO
  - _self_

params:
  config:
    name: ${resolve_default:00_SimToolRealMotionImitationSAPG05PPO,${....experiment}}
