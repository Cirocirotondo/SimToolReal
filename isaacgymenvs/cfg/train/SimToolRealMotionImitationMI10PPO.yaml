# PPO optimization profile for the SAPG08-controlled comparison. The task is
# identical to SAPG08; only the optimizer/network profile differs.
defaults:
  - SimToolRealMotionImitationMI09PPO
  - _self_

params:
  config:
    name: ${resolve_default:00_SimToolRealMotionImitationMI10PPO,${....experiment}}
